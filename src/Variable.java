public class Variable {
    public static void main(String[] args) {
//        var can be used to automatically assign data type to variable without writing it explicit, but should directly assign the value
        var address = "Indonesia";


//        final can be used to prevent change on variable
        final String lang = "Java";

        System.out.println(lang);
        System.out.println(address);
    }
}
